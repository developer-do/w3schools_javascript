x = 5;  // Assign 5 to x

elem = document.getElementById("demo");   // Find an element
elem.innerHTML = x;                       // Display x in the element

var x;  // Declare x

var x1;  // Declare x
x1 = 5;  // Assign 5 to x

elem = document.getElementById("demo1"); // Find an element
elem.innerHTML = x1;                   // Display x in the element

/* */

var x = 5;  // Initialize x
var y = 7;  // Initialize y

elem = document.getElementById("demo2");  // Find an element
elem.innerHTML = x + " " + y;             // Display x and y

var x1 = 5;  // Initialize x

elem = document.getElementById("demo3");  // Find an element
elem.innerHTML = x1 + " " + y1;             // Display x and y

var y1 = 7;  // Initialize y

var x2 = 5;  // Initialize x
var y2;      // Declare y

elem = document.getElementById("demo4");  // Find an element
elem.innerHTML = x2 + " " + y2;

y2 = 7;      // Assign 7 to y
